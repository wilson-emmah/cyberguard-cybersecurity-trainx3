'use client';
import Link from'next/link';import{useEffect,useState}from'react';import{api,logout}from'../../lib/api';
export default function Dashboard(){const[m,setM]=useState(),[a,setA]=useState([]),[s,setS]=useState(),[r,setR]=useState(),[e,setE]=useState('');useEffect(()=>{Promise.all([api('/auth/me/'),api('/attempts/me/'),api('/sessions/active/'),api('/risk/me/')]).then(([x,y,z,q])=>{setM(x);setA(y);setS(z);setR(q)}).catch(x=>setE(x.message))},[]);if(e)return <main className="container page"><p className="error">{e}</p></main>;if(!m)return <main className="container page">Loading…</main>;return <><nav className="nav"><Link className="logo" href="/">Cyber<span>Guard</span></Link><div className="links"><Link href="/training">Training</Link><Link href="/ai-coach">AI Coach</Link><Link href="/risk">Risk</Link><Link href="/leaderboard">Leaderboard</Link><button className="button red" onClick={()=>{logout();location.href='/login'}}>Logout</button></div></nav><main className="container page"><p className="eyebrow">YOUR SECURITY COMMAND CENTER</p><h1>Welcome, {m.username}</h1>{s&&<div className="resume card"><div><p className="eyebrow">RESUME TRAINING</p><h2>{s.current_scenario?.title||'Training session'}</h2><p>{s.progress}% complete · {s.score} XP saved</p><div className="progress"><i style={{width:`${s.progress}%`}}/></div><Link className="button" href="/training">Continue</Link></div>}{!s&&<div className="resume card"><div><p className="eyebrow">NEXT STEP</p><h2>Start your cybersecurity training</h2><p>Your progress will be saved automatically.</p></div><Link className="button" href="/training">Start Training</Link></div>}<div className="cards"><div className="card"><div className="stat">{m.points}</div>XP</div><div className="card"><div className="stat">{m.level}</div>Level</div><div className="card"><div className="stat">{r?.overall||0}</div>Security Score</div><div className="card"><div className="stat">{a.length}</div>Attempts</div></div><div className="grid"><div className="card"><h2>Training activity</h2>{a.length?a.slice(0,8).map(x=><p key={x.id}>{x.correct?'✓':'✗'} {x.scenario_title} · {x.points_awarded} XP</p>):<p>No attempts yet.</p>}</div><div className="card"><h2>AI Security Coach</h2><p>Ask questions, understand mistakes, and get personalized defensive cybersecurity guidance.</p><Link className="button" href="/ai-coach">Open AI Coach</Link></div></div></main></>}
