'use client';import Link from 'next/link';import {useState} from 'react';import {useRouter} from 'next/navigation';import {API,saveTokens} from '../../lib/api';
export default function Login(){const[e,setE]=useState(''),r=useRouter();async function submit(x){x.preventDefault();setE('');const f=new FormData(x.currentTarget);try{const a=await fetch(`${API}/auth/token/`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:f.get('username'),password:f.get('password')})});const d=await a.json();if(!a.ok)throw Error(d.detail||'Invalid login');saveTokens(d);const m=await fetch(`${API}/auth/me/`,{headers:{Authorization:`Bearer ${d.access}`}}).then(z=>z.json());r.push(m.role==='admin'?'/admin':'/dashboard')}catch(a){setE(a.message)}}return <main className="auth"><div className="authCard"><div className="brand">Cyber<span>Guard</span></div><p className="eyebrow">SIGN IN</p><h1>Welcome back</h1><form onSubmit={submit}><label>Username<input name="username" required/></label><label>Password<input name="password" type="password" required/></label><button className="button full">Sign In</button></form>{e&&<p className="error">{e}</p>}<p>New user? <Link href="/register">Create an account</Link></p></div></main>}
